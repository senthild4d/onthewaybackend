# This file should ensure the existence of records required to run the application in every environment.
# The data can then be loaded with: bin/rails db:seed
#
# Real-estate app: no required seed data. Legacy Vibes seeds (payment_providers,
# venue_categories, event_tags) were removed — those tables/models no longer exist.

puts "db:seed — no seeds configured for the real-estate API (skipped)."
